# GENERATED by UDAP Build Project from .udap/pipeline.yaml — do not
# edit by hand; change the spec and re-render.
name: myshortner01
'on':
  workflow_dispatch: {}
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Lint with flake8
      run: 'pip install flake8

        flake8 app/ --max-line-length=120 --ignore=E501,W503

        '
  provision:
    runs-on: ubuntu-latest
    needs:
    - lint
    steps:
    - uses: actions/checkout@v4
    - name: Terraform init & apply
      run: "cd infra\nterraform init \\\n  -backend-config=\"bucket=${{ secrets.TF_STATE_BUCKET\
        \ }}\" \\\n  -backend-config=\"key=myshortner01/terraform.tfstate\" \\\n \
        \ -backend-config=\"region=us-east-1\"\nterraform apply -auto-approve \\\n\
        \  -var=\"project_name=${{ secrets.PROJECT_NAME }}\" \\\n  -var=\"db_password=${{\
        \ secrets.DB_PASSWORD }}\" \\\n  -var=\"ssh_public_key=${{ secrets.SSH_PUBLIC_KEY\
        \ }}\"\n"
    - name: Export outputs
      run: 'cd infra

        echo "EC2_PUBLIC_IP=$(terraform output -raw ec2_public_ip)" >> $GITHUB_ENV

        echo "RDS_HOST=$(terraform output -raw rds_host)" >> $GITHUB_ENV

        '
  configure:
    runs-on: ubuntu-latest
    needs:
    - provision
    steps:
    - uses: actions/checkout@v4
    - name: Write SSH key
      run: 'mkdir -p ~/.ssh

        echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key

        chmod 600 ~/.ssh/deploy_key

        '
    - name: Install Ansible
      run: pip install ansible
    - name: Run Ansible playbook
      run: "cd ansible\nsed -i \"s/{{ ec2_public_ip }}/$EC2_PUBLIC_IP/\" inventory.ini\n\
        ansible-playbook -i inventory.ini playbook.yml \\\n  -e \"db_host=$RDS_HOST\"\
        \ \\\n  -e \"db_password=${{ secrets.DB_PASSWORD }}\" \\\n  -e \"project_name=${{\
        \ secrets.PROJECT_NAME }}\"\n"
  verify:
    runs-on: ubuntu-latest
    needs:
    - configure
    steps:
    - uses: actions/checkout@v4
    - name: Health check
      run: 'sleep 20

        curl -f http://$EC2_PUBLIC_IP/health || exit 1

        '
